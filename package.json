{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "git subtree push --prefix .output/public origin gh-pages"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^5.1.3",
    "nuxt": "^3.0.0-rc.4",
    "sass": "^1.53.0",
    "sass-loader": "^10.3.0"
  }
}
